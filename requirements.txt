# 直接依赖 — 版本由 pip freeze 锁定
# 重新生成: source .venv/bin/activate && pip freeze --exclude-editable > requirements.txt
# pylint/flake8/mypy 为开发依赖，CI 中单独 pip install

pandas==3.0.3
akshare==1.18.64
baostock==0.9.2
matplotlib==3.11.0
requests==2.34.2
Flask==3.1.3
pylint==4.0.6
flake8==7.3.0
mypy==2.1.0
openpyxl==3.1.5
fpdf2==2.8.7
playwright==1.60.0
